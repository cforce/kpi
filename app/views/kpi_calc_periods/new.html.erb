<div class="contextual">
<%= render :partial => 'kpi/kpi_common_links' %>
</div>

<h2><%=l(:label_kpi_calc_periods_new)%></h2>

<%= labelled_form_for @period do |f| %>
<%= render :partial => 'kpi_calc_periods/form', :locals => { :f => f } %>
<% end %>

<%= render :partial => 'kpi/sidebar' %>