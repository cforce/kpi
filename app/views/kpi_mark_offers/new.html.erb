<h2><%=l(:complain_or_praise)%></h2>


<%= labelled_form_for @mark_offer do |f| %>
<%= render :partial => 'kpi_mark_offers/form', :locals => { :f => f } %>
<% end %>

<%= render :partial => 'kpi/sidebar' %>